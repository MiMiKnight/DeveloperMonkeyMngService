# application.yml
# Server配置
server:
  servlet:
    context-path: /
# Spring配置
spring:
  application:
    name: DeveloperMonkeyMngService
  profiles:
    active: prod
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
